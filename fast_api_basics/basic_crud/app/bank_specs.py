def get_all_banks():
    select bank_names from table_name
    return the json result


def update_bank_spec(ifsc_first4, **kwargs):
    df = select bank_names from table_name where ifsc_first4 = ifsc_first4
    for k, v in kwargs:
        df update key with value
    from table drop the old shit
    insert the new from df


def add_new_bank(ifsc_first4, **kwargs):
    for k, v in kwargs:
        df update key with value
    insert the new from df
